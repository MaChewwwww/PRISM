name: Branch policy

on:
  pull_request:
    branches: [staging, main]
    types: [opened, edited, reopened, synchronize]

permissions:
  contents: read

jobs:
  branch-policy:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Enforce typed work branch or staging promotion flow
        env:
          BASE_BRANCH: ${{ github.event.pull_request.base.ref }}
          HEAD_BRANCH: ${{ github.event.pull_request.head.ref }}
        run: python scripts/check_branch_flow.py --head "$HEAD_BRANCH" --base "$BASE_BRANCH"
